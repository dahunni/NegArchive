fastapi==0.115.4
uvicorn[standard]==0.32.0
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
python-multipart==0.0.9
Pillow==11.0.0
# numpy and opencv-python-headless are still needed: the /preview fallback decodes
# 16-bit and unusual TIFFs that Pillow refuses (app/routers/api.py).
numpy==2.1.3
opencv-python-headless==4.10.0.84
python-dotenv==1.0.1
# M6.1: camera raws. NegPy's scan mode saves the camera's own .ARW / .NEF / .CR3 and
# neither Pillow nor OpenCV opens one; LibRaw does, through rawpy
# (app/services/rawdecode.py). The wheel bundles LibRaw, libjpeg, lcms2 and libgomp,
# so the image needs no apt package; manylinux_2_28 wheels exist for amd64 and arm64.
rawpy==0.27.1
# M3: the LAN QR code, rendered server-side as SVG. Pure Python, no C extension,
# no network access, ~60 kB.
segno==1.6.1
# M4: Code128 barcodes for labels, rendered server-side as SVG. Pure Python.
python-barcode==0.15.1
